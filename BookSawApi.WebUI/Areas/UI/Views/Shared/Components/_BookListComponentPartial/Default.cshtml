@model List<BookSawApi.WebUI.Dtos.CategoryDtos.ResultCategoryDto>

<section id="popular-books" class="bookshelf py-5 my-5">
    <div class="container">
        <div class="row">
            <div class="col-md-12">

                <div class="section-header align-center">
                    <div class="title">                      
                    </div>
                    <h2 class="section-title">Explore: Book Collection</h2>
                </div>

                <ul class="tabs">
                    <li data-tab-target="#all-genre" class="active tab">View All</li>

                    @foreach (var item in Model)
                    {
                        <li data-tab-target="#@item.CategoryName.Replace(" ", "-").ToLower()" class="tab">@item.CategoryName</li>
                    }
                </ul>

          
                <div class="tab-content">
                
                    @await Component.InvokeAsync("_AllBookListComponentPartial")

                  
                    @foreach (var item in Model)
                    {
                        <div id="@item.CategoryName.Replace(" ", "-").ToLower()" data-tab-content>
                            <div class="row">
                                @await Component.InvokeAsync("_AllBookWithCategoryComponentPartial", new { id = item.CategoryId })
                            </div>
                        </div>
                    }
                </div>

            </div>
        </div>
    </div>
</section>